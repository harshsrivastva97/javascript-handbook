export const API_URL = 'http://localhost:9000/api';

export const AUTH_URL = `${API_URL}/auth`;

export const apiUrls = {
    LOGIN: `${API_URL}/auth/login`,
    REGISTER: `${API_URL}/auth/register`,
    GOOGLE_SIGN_IN: `${API_URL}/auth/googleSignIn`,
};